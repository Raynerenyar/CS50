#include <cs50.h>
#include <stdio.h>

string organiseToArrays(string text);

int main(void)
{
    string text = get_string("Text: ");
}

string organiseToArrays(string text)
{
    int len = strlen(text);
    for (int i = 0; i < len; i++)
    {
        if (text[i] == 32)
        {
            
        }
    }
}